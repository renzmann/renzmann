#!/bin/sh

case $1 in
	flash-model-f) ;;
	icat) ;;
	ilite) echo ilite ;;
	install-cargo) ;;
	install-cs) ;;
	install-ctags) ;;
	install-fzf) ;;
	install-golang) ;;
	install-hugo) ;;
	install-jabba) ;;
	install-language-servers) ;;
	install-nvim) ;;
	install-nvm) ;;
	install-poetry) ;;
	mail) ;;
	pywhere) ;;
	sqlfmt) ;;
	trunc) ;;
esac
